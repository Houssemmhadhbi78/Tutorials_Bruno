script:post-response {
  //First line to wait 1s between each execution of request
  // await new Promise (r => setTimeout(r,1000));
  
  //Second line to sleep() pauses execution for the specified duration.
  await bru.sleep(1000);
}
