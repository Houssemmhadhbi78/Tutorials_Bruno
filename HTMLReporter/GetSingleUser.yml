info:
  name: GetSingleUser
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/users/2"
  headers:
    - name: x-api-key
      value: reqres_1f5c662008d74ffc93882dd86bcbe461
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: bru.runner.skipRequest()
    - type: tests
      code: |-
        test("status code test",function(){
          expect(res.getStatus()).to.equal(201);
        });

        test("name data type test",function(){
          expect(res.getBody().data.first_name).to.be.a('string');
        });

        test("status text check test",function(){
          expect(res.statusText).to.equal("OK");
        });
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
