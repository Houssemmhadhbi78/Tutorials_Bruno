info:
  name: Digest Authentication
  type: http
  seq: 5

http:
  method: GET
  url: https://postman-echo.com/digest-auth
  auth:
    type: digest
    username: postman
    password: password

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
