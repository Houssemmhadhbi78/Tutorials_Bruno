info:
  name: Generate Bearer Token with Script
  type: http
  seq: 6

http:
  method: POST
  url: https://airportgap.com/api/tokens
  body:
    type: json
    data: ""

runtime:
  scripts:
    - type: before-request
      code: |-
        req.setBody({
        	"email":"houssem.mhadhbi@etik.com",
        	"password":"123Houssem&&"
        });
    - type: after-response
      code: bru.setVar("tokenValue",res.body.token);
    - type: tests
      code: |-
        test("response time test",function(){
          expect(res.getResponseTime()).to.lte(1000);
        });
  assertions:
    - expression: res.responseTime
      operator: lte
      value: "1000"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
