info:
  name: Basic Auth with Headers
  type: http
  seq: 2

http:
  method: GET
  url: https://postman-echo.com/basic-auth
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
